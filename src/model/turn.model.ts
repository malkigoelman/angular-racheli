export interface Turn {
    id: string;
    day: string;              // יום (מחרוזת: ראשון, שני, שלישי...)
    hour: string;             // שעה
    customer: string;
    sellerId:string  // שם פאנית
}